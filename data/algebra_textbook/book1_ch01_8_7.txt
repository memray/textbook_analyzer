Converting A Percent To A Decimal We can see how a percent is converted to a decimal by analyzing the method that 12% is converted to a decimal. We need to introduce Percent to Decimal To convert a percent to a decimal, replace the % symbol with then divide the number by 100. This amounts to moving the decimal point 2 places to the left. Converting A Percent To A Decimal We can see how a percent is converted to a decimal by analyzing the method that 12% is converted to a decimal. We need to introduce Percent to Decimal To convert a percent to a decimal, replace the % symbol with then divide the number by 100. This amounts to moving the decimal point 2 places to the left.  || 