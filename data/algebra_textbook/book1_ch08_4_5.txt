Division Of Rational Expressions To divide one rational expression by another, we first invert the divisor then multiply the two expressions. Symbolically, if we let P,Q,R, and S represent polynomials, we can write Division Of Rational Expressions To divide one rational expression by another, we first invert the divisor then multiply the two expressions. Symbolically, if we let P,Q,R, and S represent polynomials, we can write  || 